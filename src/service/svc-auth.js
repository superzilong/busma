// import http from "../utility/util-http";
// import { apiUrl } from "../config.json";

export function svcLogin(username, password) {
  // return http.post(apiUrl + "/login", { Name: username, Password: password });
}
